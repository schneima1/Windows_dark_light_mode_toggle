These files allow you to easily toggle between dark and light mode in Windows.
toggle-darkmode.ps1 uses the powershell to switch between the modes
toggle-darkmode.bat calls the powershell script to execute it via double click and allow for hotkeys (at least when using a shortcut)

Note that both files need to be in the same folder level!
